# Vello Guardrail

This file lives in the design system bundle at [`design/docs/guardaril.md`](./docs/guardaril.md).

It sits alongside `colors.css`, `spacing.css`, and `typography.css` as the build and accessibility guardrail spec for AI-assisted Vello UI work.
